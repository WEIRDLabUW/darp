train_config:
  epochs: 345
  batch_size: 32
  cond_force_retrain: True
  model_name: "square_bc_baseline"
  optimizers:
    0:
      lr: 0.01
      weight_decay: 0.000001

model_config:
  type: "mlp"
  scale: True
  obs_horizon: 1
  act_horizon: 1
  hidden_dims:
  - 256
  - 256
  - 256
  dropout_rate: 0.0
  batch_norm: True
  assume_io_size: True
  optimizer: 0

# Expected score: 0.46